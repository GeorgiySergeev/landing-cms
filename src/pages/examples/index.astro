---
import '@styles/global.css';
import HomeIcon from '@/components/icons/HomeIcon';
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <title>Examples!</title>
    <script is:inline src="/src/utils/toggleTheme.js" type="module"></script>
    <script is:inline>
      // Initialize theme after the script is loaded
      window.addEventListener('DOMContentLoaded', function () {
        if (window.initializeTheme) window.initializeTheme();
      });
    </script>
  </head>
  <body
    class="bg-gray-100 text-gray-900 dark:bg-gray-900 dark:text-gray-100 transition-colors duration-200"
  >
    <header class="py-6 bg-blue-500 dark:bg-blue-800 text-white">
      <div class="container mx-auto p-4">
        <div class="container mx-auto flex justify-between items-center">
          <a
            href="/"
            class="flex items-center gap-2 hover:opacity-80 transition-opacity"
          >
            <HomeIcon client:load name={'Home'} />
          </a>
          <button
            onclick="window.toggleTheme()"
            class="px-4 py-2 bg-white/20 hover:bg-white/30 dark:bg-black/20 dark:hover:bg-black/30 rounded transition-colors"
          >
            Dark Mode
          </button>
        </div>
      </div>
    </header>
    <main>
      <div class="container mx-auto p-4">
        <h1 class="text-2xl font-bold">
          Here is page with landing page examples.
        </h1>
        <p class="mt-2 text-gray-600 dark:text-gray-400">
          The page is under construction.
        </p>

        <div class="mt-8 p-6 bg-white dark:bg-slate-800 rounded-lg shadow-md">
          <h2 class="text-xl font-semibold text-gray-700 dark:text-slate-200">
            Пример блока
          </h2>
          <p class="mt-2 text-gray-600 dark:text-slate-400">
            Этот блок будет менять свой фон и цвет текста в зависимости от
            выбранной темы.
          </p>
        </div>
      </div>
    </main>
  </body>
</html>
